#!/usr/bin/env sh
set -eu

if ! command -v pnpm >/dev/null 2>&1; then
	echo "pre-commit: pnpm is required to run format checks."
	exit 1
fi

echo "pre-commit: running pnpm format:check"
pnpm format:check
